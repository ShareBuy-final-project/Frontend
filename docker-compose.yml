services:
  expo:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8083:8083"
    command: npx expo start --port 8083
    # volumes:
    #   - .:/app
    environment:
      - NODE_ENV=development